[package]
name = "yas-backend"
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
rocket = "0.4.1"
serde = "1.0"
serde_json = "1.0"
serde_derive = "1.0"

[dependencies.rocket_contrib]
rocket_contrib = "0.4.1"
default-features = false
features = ["json"]
